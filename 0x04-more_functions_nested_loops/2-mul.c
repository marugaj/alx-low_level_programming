#include "main.h"

/**
 * mul - this multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply with the first number
 * Return: Result from multiplication between parameter and b parameter
 */

int mul(int a, int b)
{
	return (a * b);
}
