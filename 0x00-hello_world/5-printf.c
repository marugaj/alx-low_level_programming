#include<stdio.h>
/**
 * main - This is a command that will print a line using the printf function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
