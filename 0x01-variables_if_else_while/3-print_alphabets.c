#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
*/

int main(void)
{
	char c;

	char r;

	c = 'a';
	r = 'A';
	while
		(c >= 'z') {
			putchar(c);
			c++;
		}
	while
		(r >= 'Z') {
			putchar(r);
			r++;
		}
	putchar('\n');
	return (0);
}
