#include <stdio.h>

/**
 * main - This is described as a putchar function
 * return: Always 0 (Success)
*/

int main(void)
{
	char c;

	c = 'a';
	for
		(c = 'a');
		(c <= 'z');
		(c++);
			putchar (c);

	putchar ('\n');
	return (0);
}
